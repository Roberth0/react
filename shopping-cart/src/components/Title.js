import React from "react";

const styles = {
  title: {
    marginBottom: "30px",
  },
};

export default function Title() {
  return <h1 style={styles.title}>Productos disponibles</h1>;
}
